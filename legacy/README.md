# Legacy artifacts

`prototype-v1/` contains the original hand-edited HTML and ZIP copies. They are preserved for provenance only. Do not edit or publish them as current releases. Canonical source is `src/`; generated releases are placed in `release/`.
