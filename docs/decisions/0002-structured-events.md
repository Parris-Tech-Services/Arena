# ADR 0002: Structured events are the replay API

Status: accepted

The engine emits typed combat events. Human logs, private agent context, animation, accessibility narration, and metrics are formatters/consumers. Prose is never parsed to recover game state.
